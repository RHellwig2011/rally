node_modules

# Next.js
.next/
out/

# Environment
# Match every .env variant. The narrow list here previously missed .env.new,
# which sat untracked in the tree holding live credentials — one `git add -A`
# from being committed. Secrets belong only in .env.example, as placeholders.
.env
.env.*
!.env.example

# Testing
test-*.mjs
SESSION_*.md
STRIPE_*.md

# Build artifacts
*.log
# Incremental TypeScript build cache — rewritten on every compile, so tracking
# it produces a spurious diff in unrelated commits.
tsconfig.tsbuildinfo

.next/
boba.zip
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/compiled/

# Credential inventories / operational notes. These have held real secrets
# (plaintext JWT_SECRET, NEXTAUTH_SECRET, and a database DSN with password)
# and were tracked on a public remote. A repository is not a password manager.
NEW_CREDENTIALS_TRACKER.md
API_KEYS_AND_STATUS.csv
*CREDENTIALS*
*credentials*.csv
